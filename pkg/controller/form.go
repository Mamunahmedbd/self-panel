package controller

import (
	"github.com/mikestefanello/pagoda/pkg/types"
)

// FormSubmission represents the state of the submission of a form, not including the form itself
// This is now an alias to types.FormSubmission to break import cycles.
type FormSubmission = types.FormSubmission

