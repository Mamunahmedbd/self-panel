// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/mikestefanello/pagoda/ent/predicate"
	"github.com/mikestefanello/pagoda/ent/radacct"
)

// RadAcctQuery is the builder for querying RadAcct entities.
type RadAcctQuery struct {
	config
	ctx        *QueryContext
	order      []radacct.OrderOption
	inters     []Interceptor
	predicates []predicate.RadAcct
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RadAcctQuery builder.
func (raq *RadAcctQuery) Where(ps ...predicate.RadAcct) *RadAcctQuery {
	raq.predicates = append(raq.predicates, ps...)
	return raq
}

// Limit the number of records to be returned by this query.
func (raq *RadAcctQuery) Limit(limit int) *RadAcctQuery {
	raq.ctx.Limit = &limit
	return raq
}

// Offset to start from.
func (raq *RadAcctQuery) Offset(offset int) *RadAcctQuery {
	raq.ctx.Offset = &offset
	return raq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (raq *RadAcctQuery) Unique(unique bool) *RadAcctQuery {
	raq.ctx.Unique = &unique
	return raq
}

// Order specifies how the records should be ordered.
func (raq *RadAcctQuery) Order(o ...radacct.OrderOption) *RadAcctQuery {
	raq.order = append(raq.order, o...)
	return raq
}

// First returns the first RadAcct entity from the query.
// Returns a *NotFoundError when no RadAcct was found.
func (raq *RadAcctQuery) First(ctx context.Context) (*RadAcct, error) {
	nodes, err := raq.Limit(1).All(setContextOp(ctx, raq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{radacct.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (raq *RadAcctQuery) FirstX(ctx context.Context) *RadAcct {
	node, err := raq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RadAcct ID from the query.
// Returns a *NotFoundError when no RadAcct ID was found.
func (raq *RadAcctQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = raq.Limit(1).IDs(setContextOp(ctx, raq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{radacct.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (raq *RadAcctQuery) FirstIDX(ctx context.Context) int64 {
	id, err := raq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RadAcct entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RadAcct entity is found.
// Returns a *NotFoundError when no RadAcct entities are found.
func (raq *RadAcctQuery) Only(ctx context.Context) (*RadAcct, error) {
	nodes, err := raq.Limit(2).All(setContextOp(ctx, raq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{radacct.Label}
	default:
		return nil, &NotSingularError{radacct.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (raq *RadAcctQuery) OnlyX(ctx context.Context) *RadAcct {
	node, err := raq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RadAcct ID in the query.
// Returns a *NotSingularError when more than one RadAcct ID is found.
// Returns a *NotFoundError when no entities are found.
func (raq *RadAcctQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = raq.Limit(2).IDs(setContextOp(ctx, raq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{radacct.Label}
	default:
		err = &NotSingularError{radacct.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (raq *RadAcctQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := raq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RadAccts.
func (raq *RadAcctQuery) All(ctx context.Context) ([]*RadAcct, error) {
	ctx = setContextOp(ctx, raq.ctx, ent.OpQueryAll)
	if err := raq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RadAcct, *RadAcctQuery]()
	return withInterceptors[[]*RadAcct](ctx, raq, qr, raq.inters)
}

// AllX is like All, but panics if an error occurs.
func (raq *RadAcctQuery) AllX(ctx context.Context) []*RadAcct {
	nodes, err := raq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RadAcct IDs.
func (raq *RadAcctQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if raq.ctx.Unique == nil && raq.path != nil {
		raq.Unique(true)
	}
	ctx = setContextOp(ctx, raq.ctx, ent.OpQueryIDs)
	if err = raq.Select(radacct.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (raq *RadAcctQuery) IDsX(ctx context.Context) []int64 {
	ids, err := raq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (raq *RadAcctQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, raq.ctx, ent.OpQueryCount)
	if err := raq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, raq, querierCount[*RadAcctQuery](), raq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (raq *RadAcctQuery) CountX(ctx context.Context) int {
	count, err := raq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (raq *RadAcctQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, raq.ctx, ent.OpQueryExist)
	switch _, err := raq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (raq *RadAcctQuery) ExistX(ctx context.Context) bool {
	exist, err := raq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RadAcctQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (raq *RadAcctQuery) Clone() *RadAcctQuery {
	if raq == nil {
		return nil
	}
	return &RadAcctQuery{
		config:     raq.config,
		ctx:        raq.ctx.Clone(),
		order:      append([]radacct.OrderOption{}, raq.order...),
		inters:     append([]Interceptor{}, raq.inters...),
		predicates: append([]predicate.RadAcct{}, raq.predicates...),
		// clone intermediate query.
		sql:  raq.sql.Clone(),
		path: raq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Acctsessionid string `json:"acctsessionid,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RadAcct.Query().
//		GroupBy(radacct.FieldAcctsessionid).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (raq *RadAcctQuery) GroupBy(field string, fields ...string) *RadAcctGroupBy {
	raq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RadAcctGroupBy{build: raq}
	grbuild.flds = &raq.ctx.Fields
	grbuild.label = radacct.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Acctsessionid string `json:"acctsessionid,omitempty"`
//	}
//
//	client.RadAcct.Query().
//		Select(radacct.FieldAcctsessionid).
//		Scan(ctx, &v)
func (raq *RadAcctQuery) Select(fields ...string) *RadAcctSelect {
	raq.ctx.Fields = append(raq.ctx.Fields, fields...)
	sbuild := &RadAcctSelect{RadAcctQuery: raq}
	sbuild.label = radacct.Label
	sbuild.flds, sbuild.scan = &raq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RadAcctSelect configured with the given aggregations.
func (raq *RadAcctQuery) Aggregate(fns ...AggregateFunc) *RadAcctSelect {
	return raq.Select().Aggregate(fns...)
}

func (raq *RadAcctQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range raq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, raq); err != nil {
				return err
			}
		}
	}
	for _, f := range raq.ctx.Fields {
		if !radacct.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if raq.path != nil {
		prev, err := raq.path(ctx)
		if err != nil {
			return err
		}
		raq.sql = prev
	}
	return nil
}

func (raq *RadAcctQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RadAcct, error) {
	var (
		nodes = []*RadAcct{}
		_spec = raq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RadAcct).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RadAcct{config: raq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, raq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (raq *RadAcctQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := raq.querySpec()
	_spec.Node.Columns = raq.ctx.Fields
	if len(raq.ctx.Fields) > 0 {
		_spec.Unique = raq.ctx.Unique != nil && *raq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, raq.driver, _spec)
}

func (raq *RadAcctQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(radacct.Table, radacct.Columns, sqlgraph.NewFieldSpec(radacct.FieldID, field.TypeInt64))
	_spec.From = raq.sql
	if unique := raq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if raq.path != nil {
		_spec.Unique = true
	}
	if fields := raq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, radacct.FieldID)
		for i := range fields {
			if fields[i] != radacct.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := raq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := raq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := raq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := raq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (raq *RadAcctQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(raq.driver.Dialect())
	t1 := builder.Table(radacct.Table)
	columns := raq.ctx.Fields
	if len(columns) == 0 {
		columns = radacct.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if raq.sql != nil {
		selector = raq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if raq.ctx.Unique != nil && *raq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range raq.predicates {
		p(selector)
	}
	for _, p := range raq.order {
		p(selector)
	}
	if offset := raq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := raq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// RadAcctGroupBy is the group-by builder for RadAcct entities.
type RadAcctGroupBy struct {
	selector
	build *RadAcctQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ragb *RadAcctGroupBy) Aggregate(fns ...AggregateFunc) *RadAcctGroupBy {
	ragb.fns = append(ragb.fns, fns...)
	return ragb
}

// Scan applies the selector query and scans the result into the given value.
func (ragb *RadAcctGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ragb.build.ctx, ent.OpQueryGroupBy)
	if err := ragb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RadAcctQuery, *RadAcctGroupBy](ctx, ragb.build, ragb, ragb.build.inters, v)
}

func (ragb *RadAcctGroupBy) sqlScan(ctx context.Context, root *RadAcctQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ragb.fns))
	for _, fn := range ragb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ragb.flds)+len(ragb.fns))
		for _, f := range *ragb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ragb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ragb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RadAcctSelect is the builder for selecting fields of RadAcct entities.
type RadAcctSelect struct {
	*RadAcctQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ras *RadAcctSelect) Aggregate(fns ...AggregateFunc) *RadAcctSelect {
	ras.fns = append(ras.fns, fns...)
	return ras
}

// Scan applies the selector query and scans the result into the given value.
func (ras *RadAcctSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ras.ctx, ent.OpQuerySelect)
	if err := ras.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RadAcctQuery, *RadAcctSelect](ctx, ras.RadAcctQuery, ras, ras.inters, v)
}

func (ras *RadAcctSelect) sqlScan(ctx context.Context, root *RadAcctQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ras.fns))
	for _, fn := range ras.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ras.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ras.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
