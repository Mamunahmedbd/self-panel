// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/mikestefanello/pagoda/ent/clientuser"
	"github.com/mikestefanello/pagoda/ent/predicate"
)

// ClientUserQuery is the builder for querying ClientUser entities.
type ClientUserQuery struct {
	config
	ctx        *QueryContext
	order      []clientuser.OrderOption
	inters     []Interceptor
	predicates []predicate.ClientUser
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ClientUserQuery builder.
func (cuq *ClientUserQuery) Where(ps ...predicate.ClientUser) *ClientUserQuery {
	cuq.predicates = append(cuq.predicates, ps...)
	return cuq
}

// Limit the number of records to be returned by this query.
func (cuq *ClientUserQuery) Limit(limit int) *ClientUserQuery {
	cuq.ctx.Limit = &limit
	return cuq
}

// Offset to start from.
func (cuq *ClientUserQuery) Offset(offset int) *ClientUserQuery {
	cuq.ctx.Offset = &offset
	return cuq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cuq *ClientUserQuery) Unique(unique bool) *ClientUserQuery {
	cuq.ctx.Unique = &unique
	return cuq
}

// Order specifies how the records should be ordered.
func (cuq *ClientUserQuery) Order(o ...clientuser.OrderOption) *ClientUserQuery {
	cuq.order = append(cuq.order, o...)
	return cuq
}

// First returns the first ClientUser entity from the query.
// Returns a *NotFoundError when no ClientUser was found.
func (cuq *ClientUserQuery) First(ctx context.Context) (*ClientUser, error) {
	nodes, err := cuq.Limit(1).All(setContextOp(ctx, cuq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{clientuser.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cuq *ClientUserQuery) FirstX(ctx context.Context) *ClientUser {
	node, err := cuq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ClientUser ID from the query.
// Returns a *NotFoundError when no ClientUser ID was found.
func (cuq *ClientUserQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cuq.Limit(1).IDs(setContextOp(ctx, cuq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{clientuser.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cuq *ClientUserQuery) FirstIDX(ctx context.Context) int {
	id, err := cuq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ClientUser entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ClientUser entity is found.
// Returns a *NotFoundError when no ClientUser entities are found.
func (cuq *ClientUserQuery) Only(ctx context.Context) (*ClientUser, error) {
	nodes, err := cuq.Limit(2).All(setContextOp(ctx, cuq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{clientuser.Label}
	default:
		return nil, &NotSingularError{clientuser.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cuq *ClientUserQuery) OnlyX(ctx context.Context) *ClientUser {
	node, err := cuq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ClientUser ID in the query.
// Returns a *NotSingularError when more than one ClientUser ID is found.
// Returns a *NotFoundError when no entities are found.
func (cuq *ClientUserQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cuq.Limit(2).IDs(setContextOp(ctx, cuq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{clientuser.Label}
	default:
		err = &NotSingularError{clientuser.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cuq *ClientUserQuery) OnlyIDX(ctx context.Context) int {
	id, err := cuq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ClientUsers.
func (cuq *ClientUserQuery) All(ctx context.Context) ([]*ClientUser, error) {
	ctx = setContextOp(ctx, cuq.ctx, ent.OpQueryAll)
	if err := cuq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ClientUser, *ClientUserQuery]()
	return withInterceptors[[]*ClientUser](ctx, cuq, qr, cuq.inters)
}

// AllX is like All, but panics if an error occurs.
func (cuq *ClientUserQuery) AllX(ctx context.Context) []*ClientUser {
	nodes, err := cuq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ClientUser IDs.
func (cuq *ClientUserQuery) IDs(ctx context.Context) (ids []int, err error) {
	if cuq.ctx.Unique == nil && cuq.path != nil {
		cuq.Unique(true)
	}
	ctx = setContextOp(ctx, cuq.ctx, ent.OpQueryIDs)
	if err = cuq.Select(clientuser.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cuq *ClientUserQuery) IDsX(ctx context.Context) []int {
	ids, err := cuq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cuq *ClientUserQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, cuq.ctx, ent.OpQueryCount)
	if err := cuq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, cuq, querierCount[*ClientUserQuery](), cuq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (cuq *ClientUserQuery) CountX(ctx context.Context) int {
	count, err := cuq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cuq *ClientUserQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, cuq.ctx, ent.OpQueryExist)
	switch _, err := cuq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (cuq *ClientUserQuery) ExistX(ctx context.Context) bool {
	exist, err := cuq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ClientUserQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cuq *ClientUserQuery) Clone() *ClientUserQuery {
	if cuq == nil {
		return nil
	}
	return &ClientUserQuery{
		config:     cuq.config,
		ctx:        cuq.ctx.Clone(),
		order:      append([]clientuser.OrderOption{}, cuq.order...),
		inters:     append([]Interceptor{}, cuq.inters...),
		predicates: append([]predicate.ClientUser{}, cuq.predicates...),
		// clone intermediate query.
		sql:  cuq.sql.Clone(),
		path: cuq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ClientUser.Query().
//		GroupBy(clientuser.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (cuq *ClientUserQuery) GroupBy(field string, fields ...string) *ClientUserGroupBy {
	cuq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ClientUserGroupBy{build: cuq}
	grbuild.flds = &cuq.ctx.Fields
	grbuild.label = clientuser.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.ClientUser.Query().
//		Select(clientuser.FieldName).
//		Scan(ctx, &v)
func (cuq *ClientUserQuery) Select(fields ...string) *ClientUserSelect {
	cuq.ctx.Fields = append(cuq.ctx.Fields, fields...)
	sbuild := &ClientUserSelect{ClientUserQuery: cuq}
	sbuild.label = clientuser.Label
	sbuild.flds, sbuild.scan = &cuq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ClientUserSelect configured with the given aggregations.
func (cuq *ClientUserQuery) Aggregate(fns ...AggregateFunc) *ClientUserSelect {
	return cuq.Select().Aggregate(fns...)
}

func (cuq *ClientUserQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range cuq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, cuq); err != nil {
				return err
			}
		}
	}
	for _, f := range cuq.ctx.Fields {
		if !clientuser.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cuq.path != nil {
		prev, err := cuq.path(ctx)
		if err != nil {
			return err
		}
		cuq.sql = prev
	}
	return nil
}

func (cuq *ClientUserQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ClientUser, error) {
	var (
		nodes = []*ClientUser{}
		_spec = cuq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ClientUser).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ClientUser{config: cuq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cuq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (cuq *ClientUserQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cuq.querySpec()
	_spec.Node.Columns = cuq.ctx.Fields
	if len(cuq.ctx.Fields) > 0 {
		_spec.Unique = cuq.ctx.Unique != nil && *cuq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, cuq.driver, _spec)
}

func (cuq *ClientUserQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(clientuser.Table, clientuser.Columns, sqlgraph.NewFieldSpec(clientuser.FieldID, field.TypeInt))
	_spec.From = cuq.sql
	if unique := cuq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if cuq.path != nil {
		_spec.Unique = true
	}
	if fields := cuq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, clientuser.FieldID)
		for i := range fields {
			if fields[i] != clientuser.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cuq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cuq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cuq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cuq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cuq *ClientUserQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cuq.driver.Dialect())
	t1 := builder.Table(clientuser.Table)
	columns := cuq.ctx.Fields
	if len(columns) == 0 {
		columns = clientuser.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cuq.sql != nil {
		selector = cuq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cuq.ctx.Unique != nil && *cuq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range cuq.predicates {
		p(selector)
	}
	for _, p := range cuq.order {
		p(selector)
	}
	if offset := cuq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cuq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ClientUserGroupBy is the group-by builder for ClientUser entities.
type ClientUserGroupBy struct {
	selector
	build *ClientUserQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cugb *ClientUserGroupBy) Aggregate(fns ...AggregateFunc) *ClientUserGroupBy {
	cugb.fns = append(cugb.fns, fns...)
	return cugb
}

// Scan applies the selector query and scans the result into the given value.
func (cugb *ClientUserGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cugb.build.ctx, ent.OpQueryGroupBy)
	if err := cugb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ClientUserQuery, *ClientUserGroupBy](ctx, cugb.build, cugb, cugb.build.inters, v)
}

func (cugb *ClientUserGroupBy) sqlScan(ctx context.Context, root *ClientUserQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cugb.fns))
	for _, fn := range cugb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cugb.flds)+len(cugb.fns))
		for _, f := range *cugb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cugb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cugb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ClientUserSelect is the builder for selecting fields of ClientUser entities.
type ClientUserSelect struct {
	*ClientUserQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cus *ClientUserSelect) Aggregate(fns ...AggregateFunc) *ClientUserSelect {
	cus.fns = append(cus.fns, fns...)
	return cus
}

// Scan applies the selector query and scans the result into the given value.
func (cus *ClientUserSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cus.ctx, ent.OpQuerySelect)
	if err := cus.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ClientUserQuery, *ClientUserSelect](ctx, cus.ClientUserQuery, cus, cus.inters, v)
}

func (cus *ClientUserSelect) sqlScan(ctx context.Context, root *ClientUserQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cus.fns))
	for _, fn := range cus.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cus.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cus.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
